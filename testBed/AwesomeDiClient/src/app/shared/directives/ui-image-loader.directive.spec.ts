import { UiImageLoaderDirective } from './ui-image-loader.directive';

describe('UiImageLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new UiImageLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
